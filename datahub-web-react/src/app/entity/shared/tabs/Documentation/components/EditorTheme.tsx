import type { RemirrorThemeType } from '@remirror/theme';

export const EditorTheme: RemirrorThemeType = {
    fontSize: {
        default: '14px',
    },
    color: {
        outline: '#1890ff',
    },
};
